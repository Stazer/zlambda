[package]
name = "zlambda-scheduling"
version = "0.0.0"
authors = [ "Justus Flerlage <justus@sutsuj.com>" ]
edition = "2021"

[dependencies]
async-trait = "*"
serde = { version = "1", features = ["derive"] }
zlambda-core = { path = "../zlambda-core" }
