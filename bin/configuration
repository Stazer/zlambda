#!/bin/sh

CARGO_FLAGS="--release"
CARGO_PREFIX="RUST_LOG='trace'"
DATASETS_PATH="datasets"
DATASET_BENCHMARK_FIRST=0
DATASET_BENCHMARK_LAST=9
DATASET_COUNT=100
DATASET_FIRST=0
DATASET_LAST=`expr ${DATASET_COUNT} - 1`
DATASET_NAME="matrix"
DATASET_PATH="${DATASETS_PATH}/${DATASET_NAME}"
RESULTS_PATH="results"
